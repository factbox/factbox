<p id="notice"><%= notice %></p>

<div class="row mb-3">
  <h1><%= @artifact.title %></h1>
</div>

<div class="row mt-3">
  <%= link_to 'Back', project_path(@artifact.project), class: "btn btn-light mr-1" %>
  <%= link_to 'Edit', edit_note_path(@artifact), class: "btn btn-primary" %>
</div>
