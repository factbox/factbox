<div class="col-md-8 col-md-offset-2">
  <div class="page-head">
    <h1> Crie um novo artefato </h1>
    <small>Aqui é possível criar todos artefatos suportados</small>
  </div>

  <div class="page-content">
    <%= simple_form_for(@artifact) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :title %>
        <%= f.input :description, as: :text %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, 'Criar Artefato', class: 'btn btn-success pull-right' %>
      </div>
    <% end %>

    <%= link_to 'Back', projects_path, class: 'btn btn-default pull-left'  %>
  </div>
</div>
